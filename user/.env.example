# Optional app-only overrides.
# The repo root .env is the preferred place for shared settings.
VITE_PUBLIC_DATA_ROOT=
# Use a GitHub Raw folder URL, not a github.com/tree/... page URL.
VITE_ADMIN_API_ORIGIN=http://localhost:3000
VITE_DEV_HOST=0.0.0.0
VITE_DEV_PORT=5173
VITE_USER_BASE=/user/
