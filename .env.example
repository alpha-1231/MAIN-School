# This repo now uses app-specific env files.
# Copy:
#   admin/.env.example -> admin/.env
#   user/.env.example  -> user/.env
#
# The setup guide remains in README.md, but the runtime config is split so the
# admin panel and public user app are configured independently.
